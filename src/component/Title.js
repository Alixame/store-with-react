import React from 'react'

const Title = ({text}) => {
  return (
    <h2 className="block text-2xl font-semibold text-gray-700 dark:text-gray-200">
        {text}
    </h2>
  )
}

export default Title
